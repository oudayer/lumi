kind: pipeline
type: docker
name: build

clone:
  disable: true

steps:
  - name: clone-repo
    image: nest-pm2-app:latest
    pull: never
    commands:
      - ll
      - node -v
      - npm -v

trigger:
  branch:
    - main

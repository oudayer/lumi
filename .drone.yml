kind: pipeline
type: docker
name: build

clone:
  disable: true

steps:
  - name: clone-repo
    image: nest-pm2-app:latest
    pull: never
    commands:
      - git clone https://github.com/oudayer/lumi.git
      - cd lumi
      - pnpm install


trigger:
  branch:
    - main
